# Flutter Development Stack Deep Dives

Use these files after `skills/flutter-development/SKILL.md` detects the target Flutter state management stack.

## Available Stacks

- `riverpod.md`: Flutter applications using Riverpod providers and `AsyncValue` patterns.

## Selection Rule

Read the stack file that matches the target repository. If the repo uses BLoC, Provider, GetX, or another pattern, follow the repository convention and the general Flutter skill until a dedicated stack file is added.
