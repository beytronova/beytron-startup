# Backend Development Stack Deep Dives

Use these files after `skills/backend-development/SKILL.md` detects the target backend stack.

## Available Stacks

- `nestjs.md`: NestJS services using modules, controllers, providers, DTOs, guards, and pipes.
- `fastapi.md`: FastAPI services using routers, Pydantic models, dependencies, and Python service layers.

## Selection Rule

Read the stack file that matches the target repository. If no stack file matches, follow `skills/backend-development/SKILL.md` and existing repository conventions.
