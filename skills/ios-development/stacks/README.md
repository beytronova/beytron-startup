# iOS Development Stack Deep Dives

Use these files after `skills/ios-development/SKILL.md` detects the target iOS UI and architecture stack.

## Available Stacks

- `swiftui.md`: Native iOS applications using SwiftUI.

## Selection Rule

Read the stack file that matches the target repository. If the repo uses UIKit or another architecture, follow repository conventions and the general iOS skill until a dedicated stack file is added.
